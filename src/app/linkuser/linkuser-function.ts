// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for LinkuserFunction
 */
export interface LinkuserFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/linkuser/linkuser.
 */
export class LinkuserFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: LinkuserFunctionProps) {
    super(scope, id, {
      description: 'src/app/linkuser/linkuser.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/linkuser/linkuser.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}